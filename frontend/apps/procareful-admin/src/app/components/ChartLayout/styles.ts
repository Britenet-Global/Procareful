import { createStyles } from 'antd-style';

export const useStyles = createStyles(({ css }) => ({
  graphContainer: css`
    position: relative;
    margin-top: 0.8rem;
    display: flex;
    overflow: hidden;
  `,
}));
